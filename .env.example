# Chave da API Google Cloud Translation (Basic/v2).
# Restrinja a chave à Cloud Translation API e defina uma cota de uso no Google Cloud.
# No Render, configure esta variável no painel Environment; não envie o arquivo .env ao Git.
GOOGLE_TRANSLATE_API_KEY=
